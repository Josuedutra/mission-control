# Cockpit v0 (Next.js)
# Server-only secrets (never exposed client-side)
MC_CONVEX_SITE_URL=https://<your-deployment>.convex.site
MC_HTTP_SECRET=<your-x-mc-secret>

# Optional basic auth soft-gate (recommended if not strictly localhost).
# Format: user:pass
MC_UI_BASIC_AUTH=
