# 2026-02-13

## Ops / Hygiene
- Tabletop duplicado: transição e evidence foram feitas **localmente** (não partilhar MC_HTTP_SECRET em chat).
  - Duplicado: js7a5c9nsry43m8290mg4n221s8100ax → TRIAGED (curl /tasks/transition)
  - Evidence: /tasks/evidence requer schema `{id, actor, link, note?, sha?}` (não aceita `text`).
  - Evidence registada com link do doc:
    https://github.com/Josuedutra/mission-control/blob/main/docs/tabletop_2026-02-13.md
    Note: "DUP/obsolete → canonical js74a4rne5y5n6jh63eq6n1m6x8108mv (DONE)"

## Repo housekeeping
- SOUL.md foi substituído por versão “Jarvis (Mission Control Coordinator)” e commitado/pushado junto com notas do Day3.
  - Commit: 6e2b5af (branch feat/convex-runtime)
  - Inclui: SOUL.md, memory/2026-02-12.md, memory/day3-seed-ids.txt

## Cockpit v0 (Objective: daily operational dashboard)
- Cockpit v0 scaffold criado em `cockpit/` com Next.js + Tailwind + proxy layer (server-side injection de X-MC-SECRET via API routes). UI mínima:
  - `/` Dashboard (counters DOING/BLOCKED/APPROVAL + health)
  - `/board/[board]` Kanban por state
  - `/task/[id]` ações mínimas (transition + evidence)
  - `/agents` scaffold (não wired)

### Hardening aplicado (P0)
- Proxy: timeout 10s + erro explícito `UPSTREAM_TIMEOUT` em `cockpit/lib/mcProxy.ts`.
- API routes: `Cache-Control: no-store`.
- `/api/health` agora faz ping real ao Convex + valida secret via chamada autenticada.
- Soft-gate opcional: Basic Auth via env `MC_UI_BASIC_AUTH="user:pass"` em `cockpit/middleware.ts`.
- Mantido scope: “Pending approvals” apenas state=APPROVAL (sem inventar lógica de gates).

### Commits (2 commits, auditável)
- 747bf39 — feat(cockpit): scaffold + proxy layer (server-only secret)
- a5fac62 — feat(cockpit): dashboard + kanban + safe task actions

### Notes
- `npm install` no cockpit inicialmente falhou por conflito eslint@9 vs eslint-config-next; fix: pin eslint ^8.57.1.
- `npm install` reportou 4 vulnerabilidades (3 high, 1 critical); não foi aplicado `npm audit fix --force` (requer decisão explícita).
